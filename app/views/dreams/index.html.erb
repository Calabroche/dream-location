<div class="container mt-3">
  <div class="row">
    <div class="col-12">
      <h1>All our dreams</h1>
    </div>
  </div>

  <div class="row">
    <div class="col">
      <% if @query %>
        <p>You searched for <strong><%= @query %></strong>. We found <%= @dreams.count %> dreams out of our <%= Dream.count %> on our website.</p>
      <% else %>
        <p>All <%= Dream.count %> cars.</p>
      <% end %>
    </div>
  </div>
